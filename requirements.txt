# Pinned to exact versions (from a clean `pip install` + `pip freeze` on
# 2026-08-23) so a GitHub Actions run never silently picks up a new,
# untested release the day it ships. To refresh deliberately: install
# this file into a fresh venv, bump the direct dependency you actually
# need, reinstall, `pip freeze`, and replace this file with the new
# output -- reapplying the sys_platform marker on colorama below, since
# a plain freeze on Windows drops it (it's Windows-only per pytest's own
# metadata, and would otherwise be forced onto the Linux CI runner too).

# Direct dependencies
anthropic==1.0.0
openai==3.3.1
requests==2.34.2
feedparser==6.0.14
Pillow==12.3.0
numpy==2.5.2
PyYAML==6.0.3
Jinja2==3.1.6
python-dotenv==1.2.3
pytest==9.1.1

# Transitive dependencies, pinned for reproducibility (not imported directly)
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
charset-normalizer==3.5.1
colorama==0.4.6; sys_platform == "win32"
docstring_parser==0.18.0
feedparser-sgmllib==2.1.0
h11==0.16.0
httpcore2==2.12.0
httpx2==2.12.0
idna==3.19
iniconfig==2.3.0
jiter==0.16.0
MarkupSafe==3.0.3
packaging==26.3
pluggy==1.6.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.21.0
sniffio==1.3.1
truststore==0.10.4
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
